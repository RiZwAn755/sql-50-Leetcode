SELECT tweet_id
FROM Tweets
WHERE CHAR_LENGTH(content) > 15 // CHAR_LENGTH(col name) is used to find the length of the table on basis of char length
//while LENGTH(colname) RETURN length on the basis on=f bytes
